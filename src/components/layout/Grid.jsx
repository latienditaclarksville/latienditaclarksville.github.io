import React from 'react';

const Grid = (props) => {
  const { colNum,
          rowNum,
          componentsArray } = props;
          
  return (
    <div>
      { 
      /* Use props to set num cols in grid
         will use tailwind Grid system. 
         Grid will be passed an array of components to use*/
      }
    </div>
  );
}

export default Grid;